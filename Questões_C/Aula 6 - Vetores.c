//Escreva uma função que receba um vetor de inteiros como parâmetro e retorne um novo vetor
//com apenas os números pares contidos no vetor. Caso o vetor esteja vazio, retorne um erro.

#include<stdio.h>

int vetPar(int vet[]){
    int i = 0;
    int vetPar[];
    for (i =0 ;i <= vet; i++){
        if (vet[i] %2 == 0){
            vetPar[] = vetPar[i];
        }
    }
    return vetPar[i]
}

